# Task

The file `buggy.py` contains one bug. Running `pytest test_buggy.py` fails on `test_add`.

Your job:
1. Find the bug in `buggy.py`.
2. Fix it by editing the file directly.
3. Do NOT modify `test_buggy.py`.
4. Do NOT install anything.
5. When done, run `pytest test_buggy.py` to confirm all tests pass.